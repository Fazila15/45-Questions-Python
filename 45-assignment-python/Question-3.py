# Storing the person's name in a variable (R is in upper case to test lower case.)
person_name = "eRic" 

# Printing the name in different cases
print(person_name.lower())   # Lowercase
print(person_name.upper())   # Uppercase
print(person_name.title())   # Titlecase
