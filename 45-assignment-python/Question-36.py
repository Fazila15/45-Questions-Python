# Function to make a shirt
def make_shirt(size, message):
    print(f"The shirt is size {size} and has the message: '{message}'.")

# Call the function with sample size and message
make_shirt('M', 'Python is awesome!')
